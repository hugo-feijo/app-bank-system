import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-bank-system',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
